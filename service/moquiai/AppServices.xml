<?xml version="1.0" encoding="UTF-8"?>
<services xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="http://moqui.org/xsd/service-definition-3.xsd">

<service verb="get" noun="AvailableApps" require-authentication="false">
    <out-parameters>
        <parameter name="apps" type="List"><parameter name="app" type="Map"/></parameter>
    </out-parameters>
    <actions>
        <script><![CDATA[
            def appList = []
            // Returns Map<String, String> where key is name, value is location URL (e.g., file:/path/to/comp)
            Map componentLocations = ec.factory.getComponentBaseLocations()

            componentLocations.each { String compName, String location ->
                if (!location) return
                
                // Moqui locations often start with 'file:', we need a clean path for the File constructor
                String path = location.startsWith("file:") ? location.substring(5) : location
                File compDir = new File(path)
                
                // Check if it exists and has our 'AI-Ready' spec folder
                if (compDir.exists() && compDir.isDirectory()) {
                    boolean hasSpec = new File(compDir, "spec").exists() || new File(compDir, "specs").exists()
                    
                    if (hasSpec) {
                        // Normalize label: nursing-home -> Nursing Home
                        def label = compName.split('-').collect{ it.capitalize() }.join(' ')
                        appList.add([label: label, value: compName])
                    }
                }
            }

            // Ensure the primary target is always available for your MCE harness
            if (!appList.find { it.value == 'nursing-home' }) {
                appList.add([label: "Nursing Home Management", value: "nursing-home"])
            }

            context.apps = appList
        ]]></script>
    </actions>
</service>

    <service verb="test" noun="Dummy" type="inline">
        <in-parameters>
            <parameter name="message" type="String" default="Default Message"/>
        </in-parameters>
        <out-parameters>
            <parameter name="received" type="String"/>
            <parameter name="timestamp" type="String"/>
        </out-parameters>
        <actions>
            <script><![CDATA[
                context.received = "Echo: " + message
                context.timestamp = new java.util.Date().toString()
            ]]></script>
        </actions>
    </service>

    <service verb="get" noun="Registry" require-authentication="false">
        <in-parameters>
            <parameter name="type" required="true" default="ui-macro"/>
        </in-parameters>
        <out-parameters>
            <parameter name="registry" type="Map"/>
        </out-parameters>
        <actions>
            <script><![CDATA[
                import org.moqui.context.ExecutionContext
                import groovy.json.JsonSlurper
                ExecutionContext ec = context.ec
                
                def registryFile = new File(ec.factory.runtimePath, "component/moqui-ai/registry/${type}s.json")
                if (registryFile.exists()) {
                    context.registry = new JsonSlurper().parse(registryFile)
                } else {
                    context.registry = [categories: []]
                }
            ]]></script>
        </actions>
    </service>

    <service verb="execute" noun="GeneralRunner" type="script" location="component://moqui-ai/service/moquiai/GeneralRunner.groovy" require-authentication="false">
        <in-parameters>
            <parameter name="serviceName" required="true"/>
            <parameter name="parameters" type="Map"/>
        </in-parameters>
        <out-parameters>
            <parameter name="*" type="any"/>
        </out-parameters>
    </service>
</services>
